let num1 = prompt("Inserte el primer número");
let num2 = prompt ("Inserte el segundo número");
let resultado = (num1/num2);

if (num2 != 0)
{
    document.writeln("El resultado es " + resultado);
}
else
{
    document.writeln("No se puede dividir");
}